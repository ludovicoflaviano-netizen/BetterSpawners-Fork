package net.ludovicoflaviano.bettersfp.command;
import net.ludovicoflaviano.bettersfp.data.SpawnerStore;import org.bukkit.*;import org.bukkit.command.*;import org.bukkit.entity.Player;import org.bukkit.inventory.ItemStack;import org.bukkit.inventory.meta.ItemMeta;import org.bukkit.persistence.PersistentDataType;import java.util.*;
public final class BetterCommand implements CommandExecutor,TabCompleter {private final SpawnerStore store;private final org.bukkit.plugin.java.JavaPlugin plugin;public BetterCommand(org.bukkit.plugin.java.JavaPlugin p,SpawnerStore s){plugin=p;store=s;}public boolean onCommand(CommandSender s,Command c,String l,String[] a){if(a.length==0){s.sendMessage("§bBetterSpawners-Fork §7| /bs give <mob> [amount], /bs clear, /bs info");return true;}if(!s.hasPermission("bettersf.admin")){s.sendMessage("§cNo permission.");return true;}if(a[0].equalsIgnoreCase("clear")){int n=store.all().size();store.all().clear();store.save();s.sendMessage("§aCleared "+n+" spawners.");return true;}if(a[0].equalsIgnoreCase("info")){s.sendMessage("§bActive spawners: §f"+store.all().size());return true;}if(a[0].equalsIgnoreCase("give")&&s instanceof Player p){String mob=a.length>1?a[1].toUpperCase():"ZOMBIE";int amount=a.length>2?Math.max(1,Integer.parseInt(a[2])):1;try{org.bukkit.entity.EntityType.valueOf(mob);}catch(Exception ex){p.sendMessage("§cUnknown mob.");return true;}ItemStack it=new ItemStack(Material.SPAWNER,Math.min(64,amount));ItemMeta m=it.getItemMeta();m.setDisplayName("§b"+mob+" Spawner");m.getPersistentDataContainer().set(new NamespacedKey(plugin,"entity"),PersistentDataType.STRING,mob);it.setItemMeta(m);p.getInventory().addItem(it);p.sendMessage("§aGiven "+amount+" "+mob+" spawner(s).");return true;}s.sendMessage("§7Use /bs give <mob> [amount], /bs clear, /bs info");return true;}public List<String> onTabComplete(CommandSender s,Command c,String a,String[] x){return x.length==1?List.of("give","clear","info"):List.of();}}
